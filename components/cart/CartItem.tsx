// TODO: Wave 2 — Implement CartItem row inside CartDrawer
// Props: id, name, price, quantity, imageUrl
// Uses: zustand cart store actions (increment, decrement, remove)

export default function CartItem() {
  return null;
}
